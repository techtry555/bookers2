<!-- Books#show のページ -->

<!-- 【px-5】は、padding-左右-全sizeで5,【px-sm-0】は、padding-左右-smallsizeで5 -->
<div class ="cantainer px-5 px-sm-0">
  <div class="row">

    <!-- 部分テンプレートを呼び出す設定 -->
    <%= render 'list', book: @book %>

    <!-- 【offset-md-1】は、先頭に余白。つまり、真ん中に余白作る。 -->
      <div class="col-md-8 offset-md-1" >
        <h2>Book detail</h2>
        <table class="table">
          <!-- 表のボディ部分 -->
          <tbody>
            <tr>
              <!-- 投稿ユーザーの画像表示。link_to のテキスト部分をimage_tag() にすればOK。-->
               <!-- link_to image_tag("画像名"or メソッド, class:"クラス名"), パス-->

               <!--?? -->
              <td><%= link_to image_tag(@book.user.get_profile_image(40,40)), user_path(current_user.id) %><br><%= current_user.name %></td>
              <td><%= link_to @book.title, book_path(@book.id) %></td>
              <td><%= @book.body %></td>
              <!-- 構文  【link_to "戻る", hoge_path, class: 'btn btn-default'】 -->
              <td><%= link_to "Edit", edit_book_path(@book.id), class: 'btn btn-sm btn-success' %></td>
              <!-- ru【book_path】は、DELETE /books/:id books#destroy -->
              <td><%= link_to "Destroy", book_path, method: :delete, class: 'btn btn-sm btn-danger', "data-confirm" => "本当に消しますか？" %></td>
            </tr>
          </tbody>
        </table>
      </div>
  </div>
</div>